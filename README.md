# Demo nyxory

A small Hono server for the Forge Labs UG demo.

`GET /` returns `{ "ok": true, "service": "demo-nyxory" }`.

`GET /health` returns `ok`.

The process listens on `PORT` (default 3000) and `0.0.0.0`.
